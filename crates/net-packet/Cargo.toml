[package]
name = "net-packet"
version.workspace = true
authors.workspace = true
edition.workspace = true
license.workspace = true
description = "Zero-copy network packet parsing, modification, and creation for TCP/IP protocols"

[dependencies]
anyhow.workspace = true

[features]
default = ["std"]
std = []

[dev-dependencies]
